To run tests in the browser (assuming you're in the dev env, and you'll need Ruby):

    ../buster-util/jstdhtml

Open test/test.html in a browser

You can also run JsTestDriver from the root directory.
